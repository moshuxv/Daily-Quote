# .NET / WPF 构建产物
bin/
obj/

# WorkBuddy 项目数据（非缓存，不入库）
.workbuddy/

# 编辑器/OS
.vs/
*.user
*.suo

# 本地构建工具（Inno Setup 编译器，仅本地使用，不入库）
.tools/

# 发布产物（dotnet publish 输出，可重建，不入库）
publish/

# 安装包产物（编译生成的 setup exe，不入库）
installer/*.exe
