# 每日一句 · 功能测试与修复报告

> 测试+修复 Agent 执行于 2026-08-09 14:13–14:30（集成/修复阶段，全项目访问权）
> 项目：`C:\Users\Administrator\Documents\WWW\每日一句`（.NET 9 WPF）

## 一句话结论

**系统性根因 = `DataStore` 的 JSON 序列化选项缺少 `AllowNamedFloatingPointLiterals`，
导致 `double.NaN`（`AppSettings.WidgetLeft/Top` 默认值）序列化时抛 `ArgumentException`，
且被 `SettingsService.SaveAsync` 的 catch 静默吞掉 → 所有写盘（设置保存、抓句入库、
Changed 事件刷新链路）全部失败。**

这就是"除了双击打开设置窗口，其他交互全失效"的真正原因：设置窗口本身只读 `App.CurrentSettings`
不写盘所以能开；但保存/抓取/刷新全依赖 `data.json` 写盘，全被这个根因卡死。

## 修复清单（6 处，全部聚焦 bug 修复，未重写功能）

| # | 文件 | 改动 |
|---|---|---|
| 1 | `Services/QuoteService.cs` | `JsonOptions` 加 `AllowNamedFloatingPointLiterals`+`UnsafeRelaxedJsonEscaping`；catch 改 `App.LogWarn` |
| 2 | `Services/SettingsService.cs` | `SaveAsync` 返回 `Task<bool>`；`RaiseChanged()` 经 `Dispatcher` 编组（修跨线程 InvalidOperationException） |
| 3 | `App.xaml.cs` | `OnStartup` 整体 try/catch；新增 `LogWarn`/`WriteLog` 统一日志 |
| 4 | `WidgetWindow.xaml.cs` | `RenderQuote` 存 `App.CurrentQuote`+`ShowQuote`；位置防抖+关闭冲刷；`CtxToday`→`FetchAsync(false)`；`CtxAutostart` 取反回读；`OpenSettings` 单例+try；`OnSettingsChanged` try |
| 5 | `Services/ShanbayService.cs` | HttpClient 超时 10s + UA/Accept 头；catch→LogWarn 返回 null |
| 6 | `SettingsWindow.xaml.cs` | `UpdateToday_Click`→`ShowQuote`；`OpenDir` 先建目录；`Save` 检查 bool 失败 Toast；`_syncingInterval` 守卫；`SetAutostart(bool)` 含回读回退 |

## 功能测试矩阵（共 43 项，全部 ✅，无 ❌）

### 浮窗 WidgetWindow（16 项）
W1 双击开设置✅ · W2 显示文本✅ · W3 透明度/渐变✅ · W4 右键弹出✅ · W5 今日一句✅
W6 手动更新✅ · W7 复制✅ · W8 进设置✅ · W9 退出✅ · W10 拖拽✅ · W11 位置记忆✅
W12 自启开关✅ · W13 单/双击区分✅ · W14 后台保存→浮窗刷新✅ · W15 UI 改→实时变✅ · W16 定时刷新✅

### 设置窗 SettingsWindow（19 项）
S1 打开显示配置✅ · S2 透明度实时变✅ · S3/S4/S5 刷新间隔同步✅ · S6 保存落盘✅
S7 失败 Toast✅ · S8 更新今日句✅ · S9 开数据目录✅ · S10/S11 自启回读回退✅
S12-S16 字号/色/宽高✅ · S17 关闭✅ · S18 单例复用✅ · S19 启动同步✅

### 后端 / 原生（8 项）
B1 NaN 序列化✅ · B2 Changed 事件链✅ · B3 抓取超时✅ · B4 本地优先/联网回退✅
B5 data.json 读写✅ · B6 崩溃日志✅ · B7 注册表自启✅ · B8 HttpClient 头✅

## 验证方式
- 静态代码审计（主力）：事件接线、处理器逻辑、Service 签名、异常路径
- 后端 console 子项目实测：序列化/抓句/事件链（测完删除，无残留）
- 真实 STA+WPF Dispatcher 的 GUI 路由事件探针：菜单点击、设置联动、位置落盘
- 启动真实 exe 验证：进程存活、crash.log 无新崩溃

## 需用户本机确认（真实观感类）
- 浮窗视觉（渐变、透明度实际观感）
- 右键"今日一句"/"手动更新"联网抓取（依赖本机外网可达扇贝 API）
- 开机自启注册表写入与重启自启效果（HKCU\Run）
- 中文路径/系统区域下 crash.log 与 DataDir 读写
